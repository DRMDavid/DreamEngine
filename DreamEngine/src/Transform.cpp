#include "Transform.h"

/**
 * @brief Metodo llamado al iniciar el componente Transform.
 *        Actualmente sin implementacion.
 */
void Transform::start() {
}

/**
 * @brief Actualiza el componente Transform.
 * @param deltaTime Tiempo transcurrido desde la ultima actualizacion.
 *        Actualmente sin implementacion.
 */
void Transform::update(float deltaTime) {
}

/**
 * @brief Renderiza el componente Transform.
 * @param window Puntero inteligente a la ventana donde dibujar.
 *        Actualmente sin implementacion.
 */
void Transform::render(const EngineUtilities::TSharedPointer<Window>& window) {
}

/**
 * @brief Destruye y libera recursos del componente Transform.
 *        Actualmente sin implementacion.
 */
void Transform::destroy() {
}
